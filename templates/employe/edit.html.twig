{% extends 'admin/index.html.twig' %}

{% block title %}Edit Employe{% endblock %}

{% block body %}
    
    <h1>Edit Employe</h1>

 <div class="container">
            <div class="row">
                <div class="col-12">
    {{ include('employe/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" role="a" href="{{ path('admin') }}">back to list</a>

    {{ include('employe/_delete_form.html.twig') }}
    
                </div>
            </div>
        </div>
{% endblock %}
