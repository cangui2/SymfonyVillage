<form method="post" action="{{ path('employe_delete', {'idUtilis': employe.idUtilis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.idUtilis) }}">
    <button class="btn">Delete</button>
</form>
