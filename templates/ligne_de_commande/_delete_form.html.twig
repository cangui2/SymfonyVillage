<form method="post" action="{{ path('ligne_de_commande_delete', {'ligId': ligne_de_commande.ligId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_de_commande.ligId) }}">
    <button class="btn">Delete</button>
</form>
