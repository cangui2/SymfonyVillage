<form method="post" action="{{ path('client_delete', {'idUtilis': client.idUtilis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.idUtilis) }}">
    <button class="btn">Delete</button>
</form>
